#!/bin/bash
set -e  # exit when any command fails
set -x # show the commands

echo -e "\n### TESTING OpenFOAM ###\n"
cd $PREFIX
if [ ! -f $CONDA_PREFIX/

if [ "$(uname)" = "Darwin" ]; then
    cd $PREFIX/src
    # detach volume
    hdiutil detach volume
fi
